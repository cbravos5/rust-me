pub mod double_swap;
pub mod absolute_sum;
pub mod days_between;
pub mod clock;
pub mod parallel_reading;

fn main() {
  // absolute_sum::execute();
  // double_swap::execute();
  // days_between::execute();
  // clock::execute();
  parallel_reading::execute();
}
